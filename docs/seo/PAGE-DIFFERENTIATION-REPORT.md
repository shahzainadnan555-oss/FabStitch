# Page differentiation report

Internal editorial comparison of meaningful body text. Header, footer, and the old shared closer are not the overlap that remains. These percentages are not a Google requirement.

## What was separated

| Page job                      | Nearest page               | What used to be copied                                  | What is specific now                                                                                             |
| ----------------------------- | -------------------------- | ------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| Cotton fabric                 | Cotton for shirts          | Same discovery closer and the full cotton essay on both | The cotton page keeps fibre, construction, and watchouts. The shirt page starts from shirt constraints.          |
| Lightweight cotton            | Cotton fabric              | Same attribute essay pasted after the fibre essay       | The attribute page leads with how to judge lightweight, then the cotton construction note.                       |
| Cotton vs linen               | Either fibre page          | Fibre intros plus a shared sourcing closer              | The comparison leads with overlap, then each side, then when to start with either.                               |
| Marketplace cotton for shirts | Discover cotton for shirts | Identical "Search / Read the cloth / Inquire"           | Marketplace headings name the filter, the fibre change, and the inquiry. Discover headings name the garment ask. |

## Highest remaining pairs

- `/discover/topics/material-use/page/2/` and `/discover/topics/material-use/page/3/` (semantic_landing, 100% shingle overlap). Both still quote the same ontology paragraph. Keep both URLs. Replace the shared paragraph with the intersection only: what this garment or attribute changes about that fibre.
- `/discover/topics/material-attribute/page/2/` and `/discover/topics/material-attribute/page/3/` (semantic_landing, 100% shingle overlap). Both still quote the same ontology paragraph. Keep both URLs. Replace the shared paragraph with the intersection only: what this garment or attribute changes about that fibre.
- `/discover/lightweight-jersey-activewear/` and `/discover/lightweight-jersey-loungewear/` (material_use, 75% shingle overlap). Both still quote the same ontology paragraph. Keep both URLs. Replace the shared paragraph with the intersection only: what this garment or attribute changes about that fibre.
- `/discover/breathable-jersey-activewear/` and `/discover/breathable-jersey-loungewear/` (material_use, 75% shingle overlap). Both still quote the same ontology paragraph. Keep both URLs. Replace the shared paragraph with the intersection only: what this garment or attribute changes about that fibre.
- `/discover/lightweight-french-terry-loungewear/` and `/discover/lightweight-french-terry-workwear/` (material_use, 74% shingle overlap). Both still quote the same ontology paragraph. Keep both URLs. Replace the shared paragraph with the intersection only: what this garment or attribute changes about that fibre.
- `/discover/breathable-french-terry-loungewear/` and `/discover/breathable-french-terry-workwear/` (material_use, 74% shingle overlap). Both still quote the same ontology paragraph. Keep both URLs. Replace the shared paragraph with the intersection only: what this garment or attribute changes about that fibre.
- `/discover/stretch-jersey-activewear/` and `/discover/stretch-jersey-loungewear/` (material_use, 74% shingle overlap). Both still quote the same ontology paragraph. Keep both URLs. Replace the shared paragraph with the intersection only: what this garment or attribute changes about that fibre.
- `/discover/lightweight-polyester-activewear/` and `/discover/lightweight-polyester-outerwear/` (material_use, 74% shingle overlap). Both still quote the same ontology paragraph. Keep both URLs. Replace the shared paragraph with the intersection only: what this garment or attribute changes about that fibre.
- `/discover/breathable-polyester-activewear/` and `/discover/breathable-polyester-outerwear/` (material_use, 74% shingle overlap). Both still quote the same ontology paragraph. Keep both URLs. Replace the shared paragraph with the intersection only: what this garment or attribute changes about that fibre.
- `/discover/soft-jersey-activewear/` and `/discover/soft-jersey-loungewear/` (material_use, 74% shingle overlap). Both still quote the same ontology paragraph. Keep both URLs. Replace the shared paragraph with the intersection only: what this garment or attribute changes about that fibre.
- `/discover/stretch-french-terry-loungewear/` and `/discover/stretch-french-terry-workwear/` (material_use, 74% shingle overlap). Both still quote the same ontology paragraph. Keep both URLs. Replace the shared paragraph with the intersection only: what this garment or attribute changes about that fibre.
- `/discover/durable-jersey-activewear/` and `/discover/durable-jersey-loungewear/` (material_use, 74% shingle overlap). Both still quote the same ontology paragraph. Keep both URLs. Replace the shared paragraph with the intersection only: what this garment or attribute changes about that fibre.
- `/discover/lightweight-french-terry-activewear/` and `/discover/lightweight-french-terry-loungewear/` (material_use, 74% shingle overlap). Both still quote the same ontology paragraph. Keep both URLs. Replace the shared paragraph with the intersection only: what this garment or attribute changes about that fibre.
- `/discover/breathable-french-terry-activewear/` and `/discover/breathable-french-terry-loungewear/` (material_use, 74% shingle overlap). Both still quote the same ontology paragraph. Keep both URLs. Replace the shared paragraph with the intersection only: what this garment or attribute changes about that fibre.
- `/discover/stretch-polyester-activewear/` and `/discover/stretch-polyester-outerwear/` (material_use, 74% shingle overlap). Both still quote the same ontology paragraph. Keep both URLs. Replace the shared paragraph with the intersection only: what this garment or attribute changes about that fibre.
- `/discover/premium-jersey-activewear/` and `/discover/premium-jersey-loungewear/` (material_use, 73% shingle overlap). Both still quote the same ontology paragraph. Keep both URLs. Replace the shared paragraph with the intersection only: what this garment or attribute changes about that fibre.
- `/discover/soft-french-terry-loungewear/` and `/discover/soft-french-terry-workwear/` (material_use, 73% shingle overlap). Both still quote the same ontology paragraph. Keep both URLs. Replace the shared paragraph with the intersection only: what this garment or attribute changes about that fibre.
- `/marketplace/performance-cotton-marketplace/` and `/marketplace/sheer-cotton-marketplace/` (attributes, 73% shingle overlap). Both still quote the same ontology paragraph. Keep both URLs. Replace the shared paragraph with the intersection only: what this garment or attribute changes about that fibre.
- `/discover/soft-polyester-activewear/` and `/discover/soft-polyester-outerwear/` (material_use, 73% shingle overlap). Both still quote the same ontology paragraph. Keep both URLs. Replace the shared paragraph with the intersection only: what this garment or attribute changes about that fibre.
- `/discover/structured-jersey-activewear/` and `/discover/structured-jersey-loungewear/` (material_use, 73% shingle overlap). Both still quote the same ontology paragraph. Keep both URLs. Replace the shared paragraph with the intersection only: what this garment or attribute changes about that fibre.
- `/discover/durable-french-terry-loungewear/` and `/discover/durable-french-terry-workwear/` (material_use, 73% shingle overlap). Both still quote the same ontology paragraph. Keep both URLs. Replace the shared paragraph with the intersection only: what this garment or attribute changes about that fibre.
- `/discover/stretch-french-terry-activewear/` and `/discover/stretch-french-terry-loungewear/` (material_use, 73% shingle overlap). Both still quote the same ontology paragraph. Keep both URLs. Replace the shared paragraph with the intersection only: what this garment or attribute changes about that fibre.
- `/discover/durable-polyester-activewear/` and `/discover/durable-polyester-outerwear/` (material_use, 73% shingle overlap). Both still quote the same ontology paragraph. Keep both URLs. Replace the shared paragraph with the intersection only: what this garment or attribute changes about that fibre.
- `/discover/premium-french-terry-loungewear/` and `/discover/premium-french-terry-workwear/` (material_use, 73% shingle overlap). Both still quote the same ontology paragraph. Keep both URLs. Replace the shared paragraph with the intersection only: what this garment or attribute changes about that fibre.
- `/discover/premium-polyester-activewear/` and `/discover/premium-polyester-outerwear/` (material_use, 73% shingle overlap). Both still quote the same ontology paragraph. Keep both URLs. Replace the shared paragraph with the intersection only: what this garment or attribute changes about that fibre.
- `/marketplace/flowy-cotton-marketplace/` and `/marketplace/structured-cotton-marketplace/` (attributes, 73% shingle overlap). Both still quote the same ontology paragraph. Keep both URLs. Replace the shared paragraph with the intersection only: what this garment or attribute changes about that fibre.
- `/discover/soft-french-terry-activewear/` and `/discover/soft-french-terry-loungewear/` (material_use, 73% shingle overlap). Both still quote the same ontology paragraph. Keep both URLs. Replace the shared paragraph with the intersection only: what this garment or attribute changes about that fibre.
- `/discover/structured-french-terry-loungewear/` and `/discover/structured-french-terry-workwear/` (material_use, 73% shingle overlap). Both still quote the same ontology paragraph. Keep both URLs. Replace the shared paragraph with the intersection only: what this garment or attribute changes about that fibre.
- `/marketplace/performance-linen-marketplace/` and `/marketplace/sheer-linen-marketplace/` (attributes, 72% shingle overlap). Both still quote the same ontology paragraph. Keep both URLs. Replace the shared paragraph with the intersection only: what this garment or attribute changes about that fibre.
- `/marketplace/knit-cloth-for-outerwear/` and `/marketplace/knit-cloth-for-workwear/` (use-cases, 72% shingle overlap). Both still quote the same ontology paragraph. Keep both URLs. Replace the shared paragraph with the intersection only: what this garment or attribute changes about that fibre.

## Alias reviews, not applied

- `/discover/denim-versus-twill/` duplicates `/discover/denim-vs-twill/`. Redirect later.
- `/discover/lightweight-denim-fabric/` uses the product keyword of `/fabrics/lightweight-denim/`. Redirect later if the discover page is not teaching a wider denim family.
- `/discover/fabric-for-dresses/` and `/fabrics/dress-fabric/` answer the same commercial query. Fold the extra explanation into the hub later.
